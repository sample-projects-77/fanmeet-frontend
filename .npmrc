# Resolve peer dependency conflict: react-scripts wants TypeScript 3/4,
# while i18next and Stream packages use TypeScript 5. Legacy mode lets npm
# install anyway (runtime is fine; only peer declarations conflict).
legacy-peer-deps=true
